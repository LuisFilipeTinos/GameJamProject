using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Zoom : MonoBehaviour
{
    //public bool zoomActive;

    //public Vector3 target;

    //public Camera cam;

    //public float speed;

    //// Start is called before the first frame update
    //void Start()
    //{
    //    cam = Camera.main;
    //}

    //// Update is called once per frame
    //public void LateUpdate()
    //{
    //    if(zoomActive)
    //    {
    //        cam.ortographicSize = Math.Lerp(ortographicSize, 8, speed);
    //        cam.transform.position = Vector3.Lerp(cam.transform.position, target[1], speed);

    //    }

    //    else
    //    {
    //        cam.ortographicSize = Math.Lerp(ortographicSize, 5, speed);
    //        cam.transform.position = Vector3.Lerp(cam.transform.position, target[0], speed);
    //    }
    //}
}
